# Démarre le moteur de réécriture
RewriteEngine On
# A décommenter pour mettre en ligne sur alwaysdata
# RewriteBase /interval/

# Réécrit l'URL
RewriteRule ^([a-zA-Z0-9\-\_\/]*)$ index.php?p=$1